# stock-screener

## プロジェクトの目的
無料データのみで国内上場株の割安度・底値度を算出し、業種別に毎朝レポートする。

## 絶対制約（最優先・厳守）
- J-Quants は無料プランのみ使用。有料プラン（月額課金）へのアップグレードは禁止。
- yfinance・EDINET・JPX銘柄一覧以外を含め、有償契約・従量課金が発生するデータソース／インフラは一切採用しない（無料枠超過で自動課金されるサービスも不可）。
- 上記に反する提案（有料API、有料ホスティング等）をこちらが行った場合は、必ず指摘すること。
- 自動発注・投資助言機能は持たない。情報整理・一次スクリーニングの補助ツールに限定する。

## ディレクトリ構成
```
src/data_sources/  銘柄マスタ・yfinance・Stooq・EDINETクライアント
src/scoring/       ファンダメンタル・テクニカル・複合スコア
src/sector/        業種別集計
src/report/        レポート生成
src/llm/           定性分析（フェーズ3）
tests/             pytest
data/              ローカルDB・キャッシュ（Git管理外）
config/            重み・閾値・業種分類設定
.github/workflows/ GitHub Actions（毎朝の自動実行）
```

## 主要コマンド
- テスト: `pytest`
- Lint: `ruff check .`
- フォーマット: `ruff format .`
- 型チェック: `mypy src`
- ローカルバッチ実行: `python -m src.main`（フェーズ1実装後に整備）

## コーディング規約
- 言語: Python 3.13（pyenvで管理、`.python-version`参照）
- フォーマッタ/Linter: ruff
- 型チェック: mypy（可能な範囲で型ヒント必須）
- 外部APIクライアントはインターフェースを抽象化し、差し替え可能にする（yfinance等の非公式ライブラリ対策）

## データソースごとの制約（要点）
- yfinance: 非公式・APIキー不要。レート制限あり（目安: 1時間あたり数百リクエスト）。複数ティッカー一括取得＋リクエスト間1〜3秒スリープ＋指数バックオフ必須。
- Stooq: 【2026-08-07時点で未使用】CSV取得エンドポイントがJS Proof-of-Work型のBot対策で全面ブロックされており（`stooq.com`/`stooq.pl`とも`/__verify`への挑戦応答を要求）、単純なHTTPリクエストでは取得不可。回避コードは書かない方針のため、フォールバックなしでyfinanceのみ運用し、失敗銘柄は「データ欠損」としてレポートに明記する。将来的に他の無料代替ソースが見つかれば`PriceDataSource`インターフェース（`src/data_sources/base.py`）実装として追加する。
- EDINET API: 無料・公式。1リクエストあたり3〜5秒間隔を空けること。書類一覧取得と書類本体取得は別ジョブ化。
- JPX銘柄一覧: 月1回程度更新。data_j.xlsを定期再取得し差分検知。
- J-Quants: 日次パイプラインでは不使用（無料プランは最大12週遅延のため）。バックテスト用途でのみ将来検討。
